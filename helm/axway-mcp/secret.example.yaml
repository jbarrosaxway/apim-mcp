# Exemplo — NÃO commits com valores reais.
# Copiar e aplicar:
#   kubectl apply -f secret.example.yaml -n apim-mcp
# Depois: helm upgrade com secrets.name=axway-mcp-credentials
apiVersion: v1
kind: Secret
metadata:
  name: axway-mcp-credentials
type: Opaque
stringData:
  AXWAY_GATEWAY_USERNAME: "admin"
  AXWAY_GATEWAY_PASSWORD: "CHANGE_ME"
  AXWAY_MANAGER_USERNAME: "apiadmin"
  AXWAY_MANAGER_PASSWORD: "CHANGE_ME"
