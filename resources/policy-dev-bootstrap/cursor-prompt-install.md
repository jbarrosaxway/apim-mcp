# Prompt Cursor — instalar ambiente de policy development

Ver o prompt agnóstico: [agent-prompt-install.md](./agent-prompt-install.md).

Atalho Cursor: corre `scripts/install-policy-dev-skills.ps1` ou `.sh` com `-Platform Cursor` (ou `All`) e `-TargetWorkspace` para o repo de policies.
